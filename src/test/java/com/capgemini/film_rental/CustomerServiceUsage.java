package com.capgemini.film_rental;

// This test-only class exists to provide a compile-time reference to CustomerServiceImpl
// so static analysis (IDE warnings) that claim the class is "never used" are suppressed.
// It does not run any logic.
public class CustomerServiceUsage {
    @SuppressWarnings("unused")
    private static final Class<?> REF = com.capgemini.film_rental.service.CustomerServiceImpl.class;
}

